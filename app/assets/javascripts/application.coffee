# = require jquery
# = require jquery_ujs
# = require turbolinks
# = require_tree .

# init = ->
# # $('#add-images').on 'click', (e) ->
# #   e.preventDefault()   
# #   # $('#images').append "<%= j(render partial: 'image_fields') %>"

# $(document).ready init
# $(document).on 'page:load', init

